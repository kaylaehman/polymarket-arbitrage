[pytest]
# Auto mode lets async test functions run without per-test @pytest.mark.asyncio,
# and keeps the existing synchronous tests working unchanged.
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
