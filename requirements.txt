# Polymarket Arbitrage Trading Bot
# Python 3.10+ required

# Async HTTP/WebSocket
httpx>=0.25.0
websockets>=12.0
aiohttp>=3.9.0

# Configuration
pyyaml>=6.0.1

# Data validation (optional but recommended)
pydantic>=2.5.0

# Utilities
python-dotenv>=1.0.0

# Live trading (Polymarket CLOB order signing + Kalshi RSA request auth)
# py-clob-client handles EIP-712 order signing / L1->L2 auth for Polymarket.
# cryptography signs Kalshi API requests (RSA-PSS). Lazy-imported; only needed in live mode.
py-clob-client>=0.17.0
cryptography>=42.0.0

# Web Dashboard
fastapi>=0.104.0
uvicorn>=0.24.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Development
black>=23.0.0
mypy>=1.7.0

